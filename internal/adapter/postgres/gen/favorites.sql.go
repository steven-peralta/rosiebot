// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: favorites.sql

package gen

import (
	"context"
	"time"
)

const addFavorite = `-- name: AddFavorite :execrows
INSERT INTO favorites (guild_id, user_id, kind, slug, name, url, picture_url, added_at)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8)
ON CONFLICT (guild_id, user_id, kind, slug) DO NOTHING
`

type AddFavoriteParams struct {
	GuildID    string
	UserID     string
	Kind       string
	Slug       string
	Name       string
	Url        string
	PictureUrl string
	AddedAt    time.Time
}

func (q *Queries) AddFavorite(ctx context.Context, arg AddFavoriteParams) (int64, error) {
	result, err := q.db.Exec(ctx, addFavorite,
		arg.GuildID,
		arg.UserID,
		arg.Kind,
		arg.Slug,
		arg.Name,
		arg.Url,
		arg.PictureUrl,
		arg.AddedAt,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const listFavorites = `-- name: ListFavorites :many
SELECT guild_id, user_id, kind, slug, name, url, picture_url, added_at FROM favorites WHERE guild_id = $1 AND user_id = $2 AND kind = $3 ORDER BY added_at, slug
`

type ListFavoritesParams struct {
	GuildID string
	UserID  string
	Kind    string
}

func (q *Queries) ListFavorites(ctx context.Context, arg ListFavoritesParams) ([]Favorite, error) {
	rows, err := q.db.Query(ctx, listFavorites, arg.GuildID, arg.UserID, arg.Kind)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Favorite{}
	for rows.Next() {
		var i Favorite
		if err := rows.Scan(
			&i.GuildID,
			&i.UserID,
			&i.Kind,
			&i.Slug,
			&i.Name,
			&i.Url,
			&i.PictureUrl,
			&i.AddedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const removeFavorite = `-- name: RemoveFavorite :execrows
DELETE FROM favorites WHERE guild_id = $1 AND user_id = $2 AND kind = $3 AND slug = $4
`

type RemoveFavoriteParams struct {
	GuildID string
	UserID  string
	Kind    string
	Slug    string
}

func (q *Queries) RemoveFavorite(ctx context.Context, arg RemoveFavoriteParams) (int64, error) {
	result, err := q.db.Exec(ctx, removeFavorite,
		arg.GuildID,
		arg.UserID,
		arg.Kind,
		arg.Slug,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}
