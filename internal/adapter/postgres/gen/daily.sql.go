// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: daily.sql

package gen

import (
	"context"
	"time"
)

const getDailyWaifu = `-- name: GetDailyWaifu :one
SELECT day, slug, uuid, name, original_name, romaji_name, picture_url, likes, trash, created_at FROM daily_waifu WHERE day = $1
`

func (q *Queries) GetDailyWaifu(ctx context.Context, day time.Time) (DailyWaifu, error) {
	row := q.db.QueryRow(ctx, getDailyWaifu, day)
	var i DailyWaifu
	err := row.Scan(
		&i.Day,
		&i.Slug,
		&i.Uuid,
		&i.Name,
		&i.OriginalName,
		&i.RomajiName,
		&i.PictureUrl,
		&i.Likes,
		&i.Trash,
		&i.CreatedAt,
	)
	return i, err
}

const putDailyWaifu = `-- name: PutDailyWaifu :execrows
INSERT INTO daily_waifu (day, slug, uuid, name, original_name, romaji_name, picture_url, likes, trash)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9)
ON CONFLICT (day) DO NOTHING
`

type PutDailyWaifuParams struct {
	Day          time.Time
	Slug         string
	Uuid         string
	Name         string
	OriginalName string
	RomajiName   string
	PictureUrl   string
	Likes        int32
	Trash        int32
}

func (q *Queries) PutDailyWaifu(ctx context.Context, arg PutDailyWaifuParams) (int64, error) {
	result, err := q.db.Exec(ctx, putDailyWaifu,
		arg.Day,
		arg.Slug,
		arg.Uuid,
		arg.Name,
		arg.OriginalName,
		arg.RomajiName,
		arg.PictureUrl,
		arg.Likes,
		arg.Trash,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const replaceDailyWaifu = `-- name: ReplaceDailyWaifu :exec
INSERT INTO daily_waifu (day, slug, uuid, name, original_name, romaji_name, picture_url, likes, trash)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9)
ON CONFLICT (day) DO UPDATE SET
    slug          = EXCLUDED.slug,
    uuid          = EXCLUDED.uuid,
    name          = EXCLUDED.name,
    original_name = EXCLUDED.original_name,
    romaji_name   = EXCLUDED.romaji_name,
    picture_url   = EXCLUDED.picture_url,
    likes         = EXCLUDED.likes,
    trash         = EXCLUDED.trash,
    created_at    = now()
`

type ReplaceDailyWaifuParams struct {
	Day          time.Time
	Slug         string
	Uuid         string
	Name         string
	OriginalName string
	RomajiName   string
	PictureUrl   string
	Likes        int32
	Trash        int32
}

func (q *Queries) ReplaceDailyWaifu(ctx context.Context, arg ReplaceDailyWaifuParams) error {
	_, err := q.db.Exec(ctx, replaceDailyWaifu,
		arg.Day,
		arg.Slug,
		arg.Uuid,
		arg.Name,
		arg.OriginalName,
		arg.RomajiName,
		arg.PictureUrl,
		arg.Likes,
		arg.Trash,
	)
	return err
}
