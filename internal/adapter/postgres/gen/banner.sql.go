// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: banner.sql

package gen

import (
	"context"
	"time"
)

const getBanner = `-- name: GetBanner :one
SELECT week_start, series_slug, series_uuid, series_name, series_url, picture_url, description, characters, created_at FROM banners WHERE week_start = $1
`

func (q *Queries) GetBanner(ctx context.Context, weekStart time.Time) (Banner, error) {
	row := q.db.QueryRow(ctx, getBanner, weekStart)
	var i Banner
	err := row.Scan(
		&i.WeekStart,
		&i.SeriesSlug,
		&i.SeriesUuid,
		&i.SeriesName,
		&i.SeriesUrl,
		&i.PictureUrl,
		&i.Description,
		&i.Characters,
		&i.CreatedAt,
	)
	return i, err
}

const putBanner = `-- name: PutBanner :execrows
INSERT INTO banners (week_start, series_slug, series_uuid, series_name, series_url, picture_url, description, characters)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8)
ON CONFLICT (week_start) DO NOTHING
`

type PutBannerParams struct {
	WeekStart   time.Time
	SeriesSlug  string
	SeriesUuid  string
	SeriesName  string
	SeriesUrl   string
	PictureUrl  string
	Description string
	Characters  []byte
}

func (q *Queries) PutBanner(ctx context.Context, arg PutBannerParams) (int64, error) {
	result, err := q.db.Exec(ctx, putBanner,
		arg.WeekStart,
		arg.SeriesSlug,
		arg.SeriesUuid,
		arg.SeriesName,
		arg.SeriesUrl,
		arg.PictureUrl,
		arg.Description,
		arg.Characters,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}
